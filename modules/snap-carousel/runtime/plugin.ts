import { defineNuxtPlugin } from '#app'

export default defineNuxtPlugin(() => {
  // Плагин может расширяться, пока просто «якорь».
  // Конфиг читаем через useRuntimeConfig() в компонентах/композаблах.
})
